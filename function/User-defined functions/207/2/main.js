//Create a function that takes a number as a parameter and returns its square root. Use this function to find the square root of 3, then the square root of 4. Sum the results and output them to the console.
function squareRoot(num) {
    return Math.sqrt(num);
}
console.log(squareRoot(3) + squareRoot(4));