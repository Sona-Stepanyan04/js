//Create a function that takes a number as a parameter and returns the cube of that number. Use this function to find the cube of the number 3 and store it in the variable `res`.
function sumCube(num) {
    return num ** 3;
}
let res = sumCube(3);
console.log(res);