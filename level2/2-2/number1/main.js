//Given an array of numbers, count the number of negative numbers in this array.
const arr = [1, -2, -3, 4, 5, 6, -7, 8, 9,-8];
// let curr = 0;
// for (let i = 0; i < arr.length; i++) {
//     if (arr[i] < 0) {
//         curr += 1;
//     }
// }
//------------------------------------------------------------------------------------------------------------------------------
//------------------------------------------------------------------------------------------------------------------------------
// let negatives = arr.filter(i => i < 0);
// console.log(negatives.length,negatives);
//------------------------------------------------------------------------------------------------------------------------------
//------------------------------------------------------------------------------------------------------------------------------
const count = arr.reduce((acc,cur) => cur < 0 ? acc + 1 : acc,0);
console.log(count);